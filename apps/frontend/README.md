# Frontend (React/TypeScript)

- Run `npm install` in this directory.
- Use `npm run dev` to start the development server (Vite, port 3000).
- The app proxies API requests to the backend (`/api` → `localhost:4000`).

# Docker

- Build: `docker build -t notanda2-frontend .`
- Run: `docker run -p 3000:3000 notanda2-frontend`

# Usage

- Open http://localhost:3000 to view the app.
